name: Label Issues
on:
  issue_comment:
    types:
      - created
      - edited
  pull_request:
    types:
      - opened
  issues:
    types:
      - opened

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@7739b9ba2efcda9dde65ad1e3c2dbe65b41dfba7
      - name: label
        uses: ckotzbauer/label-command-action@73b0c72cc01a21726c5633404527c22e181a19ff
        env:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
