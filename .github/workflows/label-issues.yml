name: Label Issues
on:
  issue_comment:
    types:
      - created
      - edited
  pull_request:
    types:
      - opened
  issues:
    types:
      - opened

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@8eb1f6a495037164bea451156472f35fdd6bafc0
      - name: label
        uses: ckotzbauer/label-command-action@6cd2e262a7b850d4eab4592a0d4905a777a477e8
        env:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
