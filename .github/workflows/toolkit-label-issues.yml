name: Label issues

on:
  workflow_call:
    secrets:
      token:
        required: true

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  label-issues:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@71cf2267d89c5cb81562390fa70a37fa40b1305e
      - name: label
        uses: ckotzbauer/label-command-action@2113855f4b5d2b7caf612c54b04a37e50fe76528
        env:
          GITHUB_TOKEN: "${{ secrets.token }}"
