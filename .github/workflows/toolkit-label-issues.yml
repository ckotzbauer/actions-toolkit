name: Label issues

on:
  workflow_call:
    secrets:
      token:
        required: true

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  label-issues:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@aadec899646c8e0f34c52d9219c2faac36626b55
      - name: label
        uses: ckotzbauer/label-command-action@605973699c2ca17748c207b195eb712b8b73fff7
        env:
          GITHUB_TOKEN: "${{ secrets.token }}"
