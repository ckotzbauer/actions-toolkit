name: Label issues

on:
  workflow_call:
    secrets:
      token:
        required: true

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  label-issues:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@8ade135a41bc03ea155e62e844d188df1ea18608
      - name: label
        uses: ckotzbauer/label-command-action@11136a4aeb72c570bfd88122acbdf69b849c7ec5
        env:
          GITHUB_TOKEN: "${{ secrets.token }}"
