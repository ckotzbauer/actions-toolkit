name: Label issues

on:
  workflow_call:
    secrets:
      token:
        required: true

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  label-issues:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@8530928916aaef40f59e6f221989ccb31f5759e7
      - name: label
        uses: ckotzbauer/label-command-action@b0c43580172047ddf835fefffe65e03dd5e22e2a
        env:
          GITHUB_TOKEN: "${{ secrets.token }}"
